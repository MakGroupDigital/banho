# Firebase App Hosting configuration for Vite SPA
runConfig:
  minInstances: 0
  maxInstances: 2
  cpu: 1
  memoryMiB: 512
  concurrency: 80

# Build configuration
env:
  - variable: NODE_ENV
    value: production

# Specify the output directory from Vite build
outputDirectory: dist
